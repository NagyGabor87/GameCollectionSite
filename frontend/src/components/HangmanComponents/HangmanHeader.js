import React from 'react';

const HangmanHeader = () => {
    return (
        <div>
            <h1>Hangman</h1>
            <p>Find the hidden word by entering one letter a time</p>
        </div>
    );
};

export default HangmanHeader;
